name: Tooling test

on:
  workflow_dispatch:
  push:
    branches:
      - "dev/**"
    paths:
      - "docs/**"
      - "ptsip.yaml"
      - ".github/workflows/tooling-test.yml"
  pull_request:
    paths:
      - "docs/**"
      - "ptsip.yaml"
      - ".github/workflows/tooling-test.yml"

defaults:
  run:
    shell: cmd

permissions:
  contents: read

jobs:
  governance:
    runs-on: self-hosted
    steps:
      - name: Checkout exact source
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0

      - name: Install governance dependencies
        run: |
          py -3.12 -m pip install -r docs/governance/requirements.txt
          py -3.12 -m pip install "PTSIP==0.3.7"

      - name: Test session state synchronization
        run: py -3.12 docs/governance/test-session-state-sync.py

      - name: Test source task binding
        run: py -3.12 docs/governance/test-task-contract.py

      - name: Validate machine development documentation
        run: py -3.12 docs/governance/validate.py --root .

      - name: Validate PTSIP profile
        run: py -3.12 -m ptsip validate .
