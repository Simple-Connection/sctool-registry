# SCTool Registry Distribution 1.0.1 Improvement Plan

```text
distribution_contract_version: 1.0.1
branch: dev/1.0.1
status: COMPLETE_PENDING_MERGE_APPROVAL
current_session: null
current_session_document: null
baseline_main: a39abc174384bac3e2c2aa4f4fe68eb1caa7a645
previous_distribution_contract_version: 1.0.0
```

## 1. Purpose

Version `1.0.1` consumes the authenticated Registry access boundary established by:

```text
docs/REGISTRY_ACCESS_V1.md
docs/REGISTRY_CONTRACT_V2.md
docs/ARTIFACT_DELIVERY_V1.md
policy/registry-policy.json
```

The version goal is to make the GitHub-authenticated Registry access model consumable by the SCTool SDK/CLI and to replace transitional flat artifact URL metadata with a structured, machine-validated delivery descriptor without taking ownership of Simple Connection desktop/UI or repository lifecycle classification.

## 2. Version branch rule

All Registry sessions belonging to this distribution contract version remain on:

```text
dev/1.0.1
```

Session completion does not itself authorize a merge to `main`. The branch becomes merge-eligible only after all approved sessions and version-level validation are complete, and the actual merge still requires explicit approval.

## 3. Documentation layout

```text
docs/ver1.0.1/
├─ 1.0.1_Improvement_plan.md
└─ session_document/
   └─ ver.1.0.1_P{N}_{topic}.yaml
```

Primary session IDs are `P1`, `P2`, ... and work-unit numbering is session-local.
Registry session authority remains in this repository even when implementation code is physically stored in another repository.

## 4. Responsibility boundary

### In scope

- consuming `REGISTRY_ACCESS_V1` in the SCTool SDK boundary;
- GitHub CLI credential-store identity handling;
- canonical GitHub identity resolution;
- inherited `GH_TOKEN` / `GITHUB_TOKEN` isolation;
- private Registry artifact repository authorization probing;
- fail-closed access-state handling;
- Registry package descriptor and artifact-delivery metadata contracts;
- backend-neutral artifact content/integrity metadata;
- authenticated GitHub Release asset locator schema and validation;
- Registry JSON/schema/cross-field validation;
- Pages metadata compatibility evidence;
- Registry-owned PTSIP profile coverage and exact conformance evidence.

### Out of scope

- Simple Connection renderer/UI design;
- Simple Connection installer UX or product release-session planning;
- Simple Connection PTSIP lifecycle classification or profile migration;
- production Root trust activation or `trust/trust.json` issuance;
- per-device entitlement, commercial licensing, or application attestation;
- exposing, persisting, or extracting GitHub tokens;
- artifact binary download implementation in P1/P2;
- package/channel resolution behavior changes in P1/P2;
- activating additional artifact delivery backends in `1.0.1`.

## 5. Session index

| Session | Topic | Status | Document |
|---|---|---|---|
| P1 | Registry access SDK boundary | COMPLETE | `session_document/ver.1.0.1_P1_registry_access_sdk_boundary.yaml` |
| P2 | Artifact delivery descriptor/schema boundary | COMPLETE | `session_document/ver.1.0.1_P2_artifact_delivery_descriptor_schema_boundary.yaml` |

No additional primary session is approved for `1.0.1`.

## 6. P1 closeout result

P1 is complete.

```text
implementation repository:     Kinirin/Simple-Connection
implementation component:      program-sdk/sctool-sdk
implementation commit:         f66894b8656c52e789826de9b69876b31a928ca4
integration branch:             dev/2.2.4
integration commit:             73d1c2a421e66c255c05b4c2e3a7410b5fd4c1cb
reviewed branch head:           34b825f6acaf4eb25c27fb30aaf82c8d03af2ae2
```

Final accepted P1 evidence:

```text
SDK typecheck:                 PASS
SDK build:                     PASS
registry-access test suite:    9/9 PASS
SDK governance validation:     PASS
P1 implementation ancestry:    PASS
PTSIP doctor:                  PASS
PTSIP validate:                PASS
PTSIP inspect:                 PASS
strict conform:                PASS / CONFORMANT
blocking_gaps:                 []
non_blocking_gaps:             []
audit:                         PASS
exact SHA clean finish:        PASS
```

The temporary Simple Connection Registry P1 candidate workflow was explicitly retired and is not a maintained gate.
Registry did not modify Simple Connection's PTSIP profile, lifecycle classification, desktop/UI, or release-session ownership.

## 7. P2 closeout result

P2 is complete.

The canonical package descriptor is schema `2.0.0`:

```text
artifact
├─ target
├─ content
│  ├─ filename
│  ├─ sha256
│  └─ size
├─ delivery
│  ├─ type = github-release-asset
│  ├─ access.contract = registry-access-v1
│  └─ locator
│     ├─ repository
│     └─ assetId
├─ publishedAt
├─ contract
└─ signature
```

Registry-owned implementation evidence:

```text
Git blob identity check:       PASS (14/14)
package schema v2 regression:  PASS (14 cases)
Registry JSON validation:      PASS
Pages schema-v2 compatibility: PASS
production trust:              INACTIVE / UNCHANGED
```

The previous P2 external blocker was invalid because the Registry workflow contained an obsolete hardcoded Tool pin. The maintained Tool version is now `0.3.7`.

Correction and closeout commits:

```text
2c071c6bc769c3494d6547ff48734f6c7b34dd3d  update workflow PTSIP Tool pin to 0.3.7
c184b9602c6685295188bd05d7ea8a20c883bf94  complete Registry PTSIP component coverage
4c579207295661cdaec4e2b347f353e13fa52f96  validate PR head exact SHA
```

Exact P2 PTSIP closeout evidence from workflow run `33604260387` / run #13:

```text
exact Registry SHA:            4c579207295661cdaec4e2b347f353e13fa52f96
PTSIP Tool install:            0.3.7 PASS
historical profile binding:    0.3.6-draft @ d6995ed232e845b88d8235b851e80ab54b7804ea
profile valid:                 true
component conflicts:           0
unassigned paths:              0
scan errors:                   0
strict conform:                CONFORMANT
blocking_gaps:                 []
non_blocking_gaps:             []
diagnostics:                   []
audit:                         PASS
snapshot:                      STABLE / clean
Registry JSON validation:      PASS (packages=0 publishers=0 trust=inactive)
production trust:              INACTIVE / UNCHANGED
workflow conclusion:           SUCCESS
```

PTSIP Tool `0.3.7` explicitly accepts the Registry's historical `0.3.6-draft` Specification binding through its compatibility validation path, so the Tool version was updated without rewriting Registry lifecycle ownership.

## 8. External blockers

There are no remaining P1 or P2 external blockers.

```text
P1 external blocker: RESOLVED
P2 external blocker: RESOLVED
```

Production Trust remains a separately deferred decision and is not a blocker for completing Distribution Contract `1.0.1` development.

## 9. Version-level acceptance

The `1.0.1` version acceptance requirements are satisfied subject to the final branch-head workflow check after these closeout documents are committed:

```text
all approved session documents                  COMPLETE
all session tests/validation                    PASS
P1 external Simple Connection closeout evidence PASS
P2 Registry PTSIP closeout evidence             PASS
Registry JSON/schema validation                 PASS
Pages descriptor compatibility                  PASS
blocking_gaps                                   []
non_blocking_gaps                               []
audit                                           PASS
production trust activation                     unchanged/deferred
Simple Connection desktop/UI ownership          untouched
Simple Connection lifecycle/profile ownership   untouched
credential/private-key material in repository   NONE
```

The final closeout-document commit itself must receive the maintained Pages/PTSIP workflow PASS before `main` merge approval is requested. No further source/schema change is expected.

## 10. Merge gate

```text
version:       1.0.1
branch:        dev/1.0.1
current:       none
sessions:      COMPLETE
P1:            COMPLETE
P2:            COMPLETE
merge_to_main: AWAITING_EXPLICIT_APPROVAL
```

Do not merge automatically. After the final branch-head workflow passes, report the exact HEAD and validation evidence and request explicit merge approval.