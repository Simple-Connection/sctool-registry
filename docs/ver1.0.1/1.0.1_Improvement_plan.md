# SCTool Registry Distribution 1.0.1 Improvement Plan

```text
distribution_contract_version: 1.0.1
branch: dev/1.0.1
status: IN_PROGRESS
current_session: P1
current_session_document: docs/ver1.0.1/session_document/ver.1.0.1_P1_registry_access_sdk_boundary.yaml
baseline_main: a39abc174384bac3e2c2aa4f4fe68eb1caa7a645
previous_distribution_contract_version: 1.0.0
```

## 1. Purpose

Version `1.0.1` consumes the authenticated Registry access boundary established by:

```text
docs/REGISTRY_ACCESS_V1.md
docs/REGISTRY_CONTRACT_V2.md
policy/registry-policy.json
```

The version goal is to make the approved GitHub-authenticated Registry access model consumable by the SCTool SDK/CLI without taking ownership of Simple Connection desktop UI, installer UX, product release-session planning, or production trust activation.

## 2. Version branch rule

All Registry sessions belonging to this distribution contract version remain on:

```text
dev/1.0.1
```

Session completion does not cause a merge to `main`. The branch is merged only after every approved `1.0.1` session and the version-level acceptance gate are complete.

The implementation repository may use a matching technical branch when Registry-owned work must change code stored outside this repository. Such a branch does not transfer session authority to that repository.

## 3. Documentation layout

```text
docs/ver1.0.1/
├─ 1.0.1_Improvement_plan.md
└─ session_document/
   └─ ver.1.0.1_P{N}_{topic}.yaml
```

Rules:

1. This file owns version purpose, `current_session`, session index, acceptance, and merge state.
2. Detailed scope, exclusions, implementation evidence, validation, completion criteria, and next action live in the session YAML.
3. Session YAML files are created only when that session is approved to begin.
4. Primary session IDs are `P1`, `P2`, ...; sub-sessions use `P1.1`, `P1.2`, ... only when required.
5. Registry session authority remains in this repository even when an implementation artifact is stored in another repository.

This organization is adapted from the version/session documentation pattern used by `Kinirin/ERP_suver`, with a deliberately shallower hierarchy.

## 4. Responsibility boundary

### In scope

- consuming `REGISTRY_ACCESS_V1` in the SCTool SDK boundary;
- GitHub CLI credential-store identity handling;
- canonical GitHub identity resolution;
- inherited `GH_TOKEN` / `GITHUB_TOKEN` isolation;
- private Registry artifact repository authorization probing;
- fail-closed access-state handling;
- SDK/CLI machine-readable contracts and security tests;
- cross-repository implementation evidence required to prove the Registry contract is consumable.

### Out of scope

- Simple Connection renderer/UI design;
- Simple Connection installer UX or product release-session planning;
- production Root trust activation or `trust/trust.json` issuance;
- per-device entitlement, commercial licensing, or application attestation;
- exposing, persisting, or extracting GitHub tokens;
- package/channel update resolution or artifact binary download in P1.

## 5. Session index

| Session | Topic | Status | Document |
|---|---|---|---|
| P1 | Registry access SDK boundary | IN_PROGRESS | `session_document/ver.1.0.1_P1_registry_access_sdk_boundary.yaml` |

Additional primary sessions are added only when their scope is approved.

## 6. P1 implementation authority

P1 is the formal current session for Registry Distribution `1.0.1`.

```text
session authority repository:  Simple-Connection/sctool-registry
current_session:               P1
implementation repository:     Kinirin/Simple-Connection
implementation component:      program-sdk/sctool-sdk
implementation branch:         dev/1.0.1
implementation base:           a0c8173941c4d3fd4376c0834e6ea13951cafc96
implementation commit:         f66894b8656c52e789826de9b69876b31a928ca4
```

The user explicitly rejected making this Registry SDK contract work consume a new Simple Connection product-version P-session. Therefore the completed Simple Connection `2.2.3` session registry is not reopened and no `2.2.4` product target is invented merely to host Registry work.

The matching Simple Connection branch is an implementation carrier only. Registry `P1` remains the planning and completion authority.

P1 work units are:

```text
W5  RegistryAccess result/types
W6  GitHub CLI identity adapter
W7  private repository authorization probe
W8  negative/security tests
```

## 7. Current validation state

Implementation has been committed to the external Program SDK branch. A local reconstruction of the new isolated TypeScript module and Node adapter produced:

```text
TypeScript compile: PASS (local tsc 5.8.3 reconstruction)
registry access tests: 9/9 PASS
```

The canonical package declares TypeScript `7.0.1-rc`; exact repository commands and applicable remote PTSIP/SDK-plane gates remain required before P1 closeout. The existing GitHub workflow runs automatically for PRs and `main` pushes, not ordinary `dev/1.0.1` pushes, so no remote CI result is claimed yet.

## 8. Version-level acceptance

Before `dev/1.0.1` may merge to `main`:

```text
all approved session documents                  COMPLETE
all session tests/validation                    PASS
Registry JSON/schema validation                 PASS
PTSIP applicable gates                          PASS
blocking_gaps                                   []
non_blocking_gaps                               []
audit                                           PASS
production trust activation                     unchanged/deferred
Simple Connection desktop/UI ownership          untouched
Simple Connection product session history       not reopened for Registry work
credential/private-key material in repository   NONE
```

## 9. Merge gate

```text
version:       1.0.1
branch:        dev/1.0.1
current:       P1
sessions:      IN_PROGRESS
merge_to_main: BLOCKED
```

`merge_to_main` becomes eligible only after every approved session is complete and the version-level acceptance above is satisfied.
