# SCTool Registry Distribution 1.0.2 Improvement Plan

```text
distribution_contract_version: 1.0.2
branch: dev/1.0.2
status: IN_PROGRESS
current_session: P2
current_session_document: session_document/ver.1.0.2_P2_registry_client_sdk_descriptor_resolution.yaml
baseline_main: c3e881742177aec3eae71d2c336801b6b52c74e5
previous_distribution_contract_version: 1.0.1
```

## 1. Purpose

Distribution `1.0.2` establishes the Registry-owned SDK boundary that Simple Connection uses to consume Registry metadata, authenticated artifact access, delivery resolution, integrity verification, and update candidates.

This version explicitly separates two SDK responsibilities:

```text
Kinirin/Simple-Connection/program-sdk/sctool-sdk
= SCTool Authoring SDK
= coding-agent/developer support for designing, validating, building, testing,
  and packaging a developer's own tool as .sctool

Simple-Connection/sctool-registry/packages/registry-client-sdk
= Registry Client SDK
= Simple Connection integration SDK for Registry metadata/access/delivery clients
```

The two SDKs have different users, release ownership, compatibility obligations, and runtime roles. Distribution `1.0.2` uses the **two-SDK model**.

## 2. 1.0.1 ownership correction

Registry Distribution `1.0.1` implemented `REGISTRY_ACCESS_V1` under the Simple Connection SCTool Authoring SDK. That implementation remains valid historical evidence, but Registry client ownership is now migrated into the Registry Client SDK.

The Simple Connection copy is not removed yet. Removal happens only after a later Simple Connection integration session proves that the product consumes the Registry Client SDK replacement.

Historical `1.0.1` session evidence is preserved and is not rewritten.

## 3. Fixed Registry contracts

`1.0.2` starts from released Registry `main`:

```text
c3e881742177aec3eae71d2c336801b6b52c74e5
```

Canonical inputs remain:

```text
docs/REGISTRY_CONTRACT_V2.md
docs/REGISTRY_ACCESS_V1.md
docs/ARTIFACT_DELIVERY_V1.md
schemas/package.schema.json
policy/registry-policy.json
```

The Registry Client SDK implements these contracts. It does not redefine publisher authoring or `.sctool` creation semantics.

## 4. Repository responsibility split

### Registry repository owns

- Registry Client SDK source and compatibility boundary;
- Registry access state model and GitHub CLI access adapter semantics;
- Registry package descriptor parsing and channel/version/target resolution;
- delivery discriminator and exact artifact locator resolution;
- authenticated artifact retrieval contract implementation;
- content filename/size/SHA-256 verification;
- client-facing normalized update candidate/result types;
- tests proving Registry contract conformance.

### Simple Connection repository owns

- SCTool Authoring SDK for developer/coding-agent tool creation;
- Desktop integration of the Registry Client SDK;
- local install state, activation, rollback, runtime reconcile and UI;
- its own PTSIP/profile and product path policy.

### Forbidden ownership mixing

- Registry Client SDK must not become an SCTool build/scaffold/package authoring SDK.
- Simple Connection Authoring SDK must not remain the long-term owner of Registry access/download/update client behavior.
- Registry does not directly edit Simple Connection PTSIP lifecycle declarations.

## 5. Registry Client SDK boundary

Approved canonical path:

```text
packages/registry-client-sdk/
```

Package identity:

```text
@simple-connection/sctool-registry-client-sdk
version: 0.1.0
publication: private / not yet approved for external package publication
```

`packages/README.md` makes the namespace split explicit:

```text
packages/{packageId}.json
= accepted Registry package descriptor

packages/registry-client-sdk/**
= Registry Client SDK implementation
```

The Registry Client SDK is a dedicated PTSIP `PRODUCT` component and is validated as part of the Registry Pages/PTSIP workflow.

## 6. Session index

| Session | Topic | Status | Document |
|---|---|---|---|
| P1 | SDK responsibility split + Registry Client SDK boundary/access migration | COMPLETE | `session_document/ver.1.0.2_P1_simple_connection_descriptor_consumer.yaml` |
| P2 | Registry Client SDK descriptor/channel/version/target resolution | VALIDATION_READY | `session_document/ver.1.0.2_P2_registry_client_sdk_descriptor_resolution.yaml` |

P2 was explicitly approved and materialized before implementation. Its implementation is complete through the recorded implementation head; closeout remains blocked on exact-checkout Registry and PTSIP gates.

Proposed later sessions:

```text
P3 GitHub Release exact asset resolution + authenticated retrieval
P4 artifact integrity/update-candidate boundary
P5 Simple Connection integration + misplaced Registry client surface removal
P6 install/activation/rollback/runtime/UI E2E closeout
```

A proposed session becomes active only after approval and materialization of its session YAML.

## 7. P1 closeout evidence

P1 established the two-SDK architecture and created the Registry Client SDK.

Architecture / correction commits:

```text
b888f3a9460c54ff7c41e716c8b96fe0fc23144c  correct 1.0.2 SDK ownership boundary
1a3f57d74a2a4047e8495707f239cf2917a0ba79  establish Registry Client SDK boundary
4d7da1c9f8c6387d60e88ba453d7de4717c7ba35  expose PTSIP validation diagnostics
e3e62e800edd9b2d73c1febde52e4cfd78299ed4  avoid empty future Registry catalog selector
41021bdfd08995c60d906316e2f056458cbb3f60  keep Product SDK tests dependency-free
e3b0c14d6dd79dc7711bc1cda604eb2895905a4f  migrate Registry access into client SDK
```

Exact validation:

```text
run #21 / 33614205637 @ 41021bdfd...
  SDK boundary             PASS
  PTSIP                    PASS / CONFORMANT
  Registry JSON            PASS

run #22 / 33614696061 @ e3b0c14d...
  SDK boundary             PASS
  Registry Access V1 tests PASS
  PTSIP                    PASS / CONFORMANT
  Registry JSON            PASS
```

Production Trust remained inactive and no Pages deployment was activated.

The migrated access boundary keeps `GH_TOKEN`/`GITHUB_TOKEN` isolation and stable Registry Access V1 states. GitHub CLI process execution is injected by the host instead of importing `node:child_process` into the Registry Client SDK Product component.

## 8. P2 active scope and implementation state

P2 implements the Registry-owned consumer boundary for package descriptor schema `2.0.0` through deterministic target selection.

```text
package descriptor validation
-> default/explicit channel resolution
-> concrete version resolution
-> platform-arch target resolution
-> content/delivery metadata preservation
```

Implementation head before documentation gate update:

```text
1b544eb300e4a67b711f2baad9c40b37ae62e3c2
```

Implemented public surfaces:

```text
@simple-connection/sctool-registry-client-sdk/package-descriptor
@simple-connection/sctool-registry-client-sdk/resolution
```

Runtime `.mjs` modules use matching `.d.mts` ESM declaration modules. Local isolated validation passed descriptor regression cases, deterministic resolution cases, syntax checks, and a TypeScript NodeNext declaration consumer. Exact-checkout Registry JSON and PTSIP gates remain pending and are the only P2 closeout gate currently recorded.

P2 does not perform live GitHub Release lookup, authenticated payload retrieval, downloaded-byte verification, update-candidate normalization, or Simple Connection integration. Those remain later-session responsibilities.

## 9. Version-level target

The remaining end-to-end target is:

```text
signed/public Registry metadata
-> Registry Client SDK
-> package/channel/version/target resolution
-> registry-access-v1 authorization
-> exact GitHub Release asset resolution
-> artifact retrieval
-> filename/size/SHA-256 verification
-> verified update candidate
-> Simple Connection local install/activation/runtime reconcile
```

The SCTool Authoring SDK is not part of this consumer runtime chain.

## 10. Production trust boundary

Production Root trust activation remains separately deferred. `1.0.2` SDK work does not activate `trust/trust.json` or Root/Distribution signing by itself.

## 11. Merge gate

```text
version:       1.0.2
branch:        dev/1.0.2
current:       P2
P1:            COMPLETE
P2:            VALIDATION_READY
next:          P2_EXACT_REPOSITORY_VALIDATION
merge_to_main: BLOCKED
```

Do not merge until all approved sessions, Registry validation, applicable PTSIP gates, and external Simple Connection integration evidence are complete.
